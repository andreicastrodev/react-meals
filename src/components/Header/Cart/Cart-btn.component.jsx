import React from "react";
import styles from "./Cart-btn.module.css"
const CartButton = () => (
  <React.Fragment>
    <button className={styles.cartBtn}>Cart</button>
  </React.Fragment>
);

export default CartButton;
